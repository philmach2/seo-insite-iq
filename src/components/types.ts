export interface AuditResult {
    //   Define your audit result interface
        pageTitle: string;
        metaDescription: string;
        headers: string[];
        mobileFriendly: boolean;
        // Add more properties as needed for your SEO audit
    }
